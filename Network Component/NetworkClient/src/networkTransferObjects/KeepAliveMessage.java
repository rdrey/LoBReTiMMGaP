package networkTransferObjects;

//Dummy class which is never actually sent. Used to signify that a keep alive bit should be written to the stream.
public class KeepAliveMessage extends NetworkMessage {

}
